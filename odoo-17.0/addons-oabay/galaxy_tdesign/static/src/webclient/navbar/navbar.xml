<?xml version="1.0" encoding="utf-8"?>
<templates id="template" xml:space="preserve">

  <t t-inherit="web.NavBar" t-inherit-mode="extension" owl="1">
    <xpath expr="//nav[hasclass('o_main_navbar')]" position="replace">
      <nav class="o_main_navbar d-flex justify-content-between" 
        t-on-dropdown-item-selected="onNavBarDropdownItemSelection" data-command-category="navbar">
        <div class="d-flex">
          <div class="top_heading">
            <!-- Apps Menu -->
            <t t-call="web.NavBar.AppsMenu">
              <t t-set="apps" t-value="menuService.getApps()" />
            </t>
            <!-- App Brand -->
            <!-- <DropdownItem
              t-if="currentApp"
              href="getMenuItemHref(currentApp)"
              t-esc="currentApp.name"
              class="'o_menu_brand d-none d-md-block'"
              dataset="{ menuXmlid: currentApp.xmlid, section: currentApp.id }"
              onSelected="() => this.onNavBarDropdownItemSelection(currentApp)"
            /> -->
            <div class="o_main_navbar_brand" />
          </div>
        </div>
        <!-- Current App Sections -->
        <t t-if="currentAppSections.length" t-call="web.NavBar.SectionsMenu">
          <t t-set="sections" t-value="currentAppSections" />
        </t>
        <!-- Systray -->
        <div class="o_menu_systray d-flex flex-shrink-0 ms-auto" role="menu">
          <t t-foreach="systrayItems" t-as="item" t-key="item.key">
            <!-- This ensures the correct order of the systray items -->
            <div t-att-data-index="item.index"/>
            <ErrorHandler onError="error => this.handleItemError(error, item)">
              <t t-component="item.Component" t-props="item.props"/>
            </ErrorHandler>
          </t>
        </div>
      </nav>
      <t t-set="menu_setting" t-value="sessionMenuSetting"/>
      <t t-set="item_id" t-value="menuService.getCurrentItemId()"/>
      <div class="sidebar_panel" id="sidebar_panel">
        <div class="sidebar scroll_menu">
          <div class="sidebar_head first-div">
            <div class="sidebar_head_menu">
              MENU
            </div>
            <div class="sidebar_close">
              <a id="closeSidebar" style="cursor: pointer;">
                <img src="/galaxy_tdesign/static/src/img/icons/close.png"/>
              </a>
            </div>
          </div>
          <ul class="sidebar_menu">
            <t t-set="current_app" t-value="currentAppId"/>
            <t t-foreach="menuService.getApps()" t-as="app" t-key="app.id">
              <li data-toggle="tooltip" data-placement="right" t-att-title="app.name">
                <a id="appMenu" role="menuitem" t-attf-href="#menu_id={{app.id}}" class="nav-link menu_dispaly">
                  <div>
                    <img class="sidebar_img" t-att-src="getWebIcon(app)"/>
                    <span class="sidebar_app_name">
                      <t t-esc="app.name"/>
                    </span>
                  </div>
                  <div t-if="app.children.length">
                    <div id="menu_icon" style="font-size: 20px;" class="fa fa-angle-down" 
                      t-att-class="(current_app.id === app.appID) ? (menu_setting === 'foldadd' ? 'menu_fold' : '') : (menu_setting === 'expandall' ? '' : 'menu_fold')"></div>
                  </div>
                </a>
                <ul id="menuItems" t-att-class="(current_app.id === app.appID) ? (menu_setting === 'foldadd' ? 'menu_passive' : '') : (menu_setting === 'expandall' ? '' : 'menu_passive')">
                  <t t-if="menuService.getMenuAsTree(app.id).childrenTree" t-foreach="menuService.getMenuAsTree(app.id).childrenTree" t-as="child" t-key="child.id">
                    <li data-toggle="tooltip" data-placement="right" t-att-title="child.name">
                      <a role="menuitem" t-attf-href="#menu_id={{child.id}}" class="nav-link" t-att-class="(item_id === child.id) ? 'active' : ''">
                        <t t-set="child_icon" t-value="getWebIcon(child)"/>
                        <t t-if="child_icon">
                          <img class="sidebar_img" t-att-src="child_icon"/>
                        </t>
                        <span class="sidebar_app_name" t-esc="child.name"/>
                      </a>
                    </li>
                  </t>
                </ul>
              </li>
            </t>
          </ul>
        </div>
      </div>
    </xpath>
  </t>
  <t t-inherit="web.NavBar.AppsMenu" t-inherit-mode="extension" owl="1">
    <xpath expr="//Dropdown" position="replace">
      <li class="dropdown">
        <a id="openSidebar" style="cursor: pointer;">
          <div class="c_sidebar_active">
            <div class="c_dots_menu"/>
          </div>
        </a>
        <a id="closeSidebar" style="cursor: pointer;">
          <div class="c_sidebar_passive">
            <div class="c_dots_menu_toggled "/>
          </div>
        </a>
      </li>
    </xpath>
  </t>
</templates>
